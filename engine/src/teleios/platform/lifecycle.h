#ifndef TELEIOS_PLATFORM_LIFECYCLE
#define TELEIOS_PLATFORM_LIFECYCLE

#include "teleios/types.h"

b8 tl_platform_initialize(void);
b8 tl_platform_terminate(void);

#endif // TELEIOS_PLATFORM_LIFECYCLE